# Operations Refresh Audit Model

Append-only JSONL history records run ID, trigger, Windows identity, quiet
window decision, timestamps, overall result, current/finished steps,
per-dataset result, duration, counts, before/after run identity where
available, failure reason, retention state, and bounded evidence identity.

No credentials are stored. Browser responses do not expose unrestricted
source or local paths.
